#include "main.h"
 
/**
 * _abs - computes the absolute
 * @n: the integrer tgats woyld be checked
 * Return: this is the absolute value of int
 */
int _abs(int n)
{
        	if (n >= 0)
        	{
                    	return (n);
        	}
        	return (-n);
}

