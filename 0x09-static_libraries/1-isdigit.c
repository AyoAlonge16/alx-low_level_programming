#include "main.h"

                    	

/**

 * _isdigit - this checks for numbers 

 * @c: the integers that would be checked

 * Return: 1 if True, that is if C is a digit, Otherwise 0

 */

int _isdigit(int c)

{

        	return (c >= '0' && c <= '9');

}
